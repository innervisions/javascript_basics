// #09 - Variable Declarations

console.log(a);

var a = 1;

// Console will log undefined.
// The declaration of a is hoisted to the top in the creation phase,
// however the assignment to 1 still takes place after console.log is called.
